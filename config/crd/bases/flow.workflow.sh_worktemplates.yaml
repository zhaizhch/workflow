---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: worktemplates.flow.workflow.sh
spec:
  group: flow.workflow.sh
  names:
    kind: WorkTemplate
    listKind: WorkTemplateList
    plural: worktemplates
    shortNames:
    - jt
    singular: worktemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkTemplate is the Schema for the worktemplates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkTemplateSpec defines the desired state of WorkTemplate
              with GVK
            properties:
              gvr:
                description: GVR is the GroupVersionResource of the Job
                properties:
                  group:
                    type: string
                  resource:
                    type: string
                  version:
                    type: string
                required:
                - group
                - resource
                - version
                type: object
              jobSpec:
                description: JobSpec is the specification of the Job
                type: object
                x-kubernetes-preserve-unknown-fields: true
            type: object
          status:
            description: WorkTemplateStatus defines the observed state of WorkTemplate
            properties:
              jobDependsOnList:
                description: JobDependsOnList is the list of jobs that this job depends
                  on
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
